#include "StreamInfo.h"

// StreamInfo is a POD aggregate; no out-of-line members are needed right now.
// This translation unit exists so the build system has a stable target for
// future additions (validators, serializers, etc.).

namespace vsa {} // namespace vsa
